export enum KeyPurposes {
  MANAGEMENT = 1,
  DELEGATE_KEY = 2,
  CLAIM_SIGNER = 3,
}

export enum Schemes {
  ECDSA = 1,
}

export enum PreDefinedClaimKeys {
  ACCOUNT_TYPE = "ACCOUNT_TYPE",
  ACCOUNT_ID = "ACCOUNT_ID",
}
